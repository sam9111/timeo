name: My Workflow
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Markdown Accessibility
        uses: sam9111/markdown-accessibility-helper@v1.0.7
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
